name: fixture
on:
  pull_request:
  merge_group:
jobs:
  validate:
    runs-on: ubuntu-24.04
    steps:
      - name: the generated ledger matches its sources
        if: ${{ github.event.repository.owner.login == 'nobody' }}
        run: ./scripts/check-generated-ledger.sh
