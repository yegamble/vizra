# Execution plan: VZ-CI-002 evidence discipline — PR #4 F-1/F-2 (CLAIMS.md line numbers)

## Context
- Repo yegamble/vizra (meta), PR #4, branch `feat/m0-compose-topology`, base head
  `ddec39c34fe92857b03c40a21b9645dec96c50c5`.
- Worktree `/Users/yosefgamble/github/vizra/.claude/worktrees/meta-pr4-f1`, local branch
  `f1/meta-pr4-compose-topology` tracking `origin/feat/m0-compose-topology` (the branch name itself
  is checked out in the `meta-compose` worktree, so a distinct local name is used; push is
  `HEAD:feat/m0-compose-topology`, fast-forward, no force).
- Findings: `docs/evidence/warroom/2026-09-21-meta-pr4-compose-topology-VERIFY.md`,
  section "Re-confirmation of docs delta at ddec39c", F-1 (REQUIRED) and F-2 (NIT).

## Acceptance
- F-1 option (a): the six line numbers in CLAIMS.md "Known weak spots" are computed from
  `scripts/ci-required-guard.sh` and `scripts/ci-required-select.sh`, so that the verifier's M5
  (insert a line after the shebang of both) makes `claims.py --check` exit 1; restored → exit 0.
  A pattern that matches nothing is refused (exit 2).
- F-2: the "does NOT enforce" list names the "What it does NOT guarantee" cell; "part of the
  local lane" replaced by: recorded in `local-run.txt`, not one of the eight required local-lane
  commands in COMMANDS.md.
- CLAIMS.md regenerated by `claims.py`, not by hand. PR body sentences accurate.
- Non-goals: fixing the pipe races in the two scripts; COMMANDS.md §8 (which still quotes the same
  six numbers in hand-written prose, with no guarantee attached) is left for the chair.

## Implementation
`docs/evidence/compose-topology/claims.py`: `WEAK_SPOT_LINES` (path, regex), `resolve_lines()`,
`cite_lines()`; header sentences rewritten; weak-spot bullet uses the computed lists and names both
pipe shapes (`| grep -q` in the guard, `| head -n 1` in the selector). CLAIMS.md regenerated.

## Verification
`python3 docs/evidence/compose-topology/claims.py --check`, `./scripts/check-generated-ledger.sh`,
`./scripts/check-quality-json.py`, `./scripts/check-doc-links.py`, `./scripts/ci-required-guard.sh`;
M5 red/green in a scratch clone of the commit; CI `validate` + `ci-required` on the pushed head.

## Progress and evidence
(appended below)

## Blockers and handoff
None at start.

### 2026-09-23 — implemented at 8e2a86742f1df87091f1891d5b4ed407955b57f7 (IMPLEMENTED, not VERIFIED)
- Option (a) taken. Diff vs ddec39c: CLAIMS.md 11 (+6/-5 lines), claims.py 64; 2 files, 62 insertions, 13 deletions.
- Computed lists equal the old hand-written ones at this SHA: guard 80, 249, 348, 423, 440; select 56.
- Worktree checks (macOS arm64, Python 3.9.6): claims.py --check 0 (47 claims); check-generated-ledger.sh 0
  (191 reqs, byte-identical UTF-8 + C/POSIX); check-quality-json.py 0 (4 files, 287 refs/204 distinct/14 docs);
  check-doc-links.py 0 (95 md files; compose comments 6 paths/8 files); ci-required-guard.sh 0 (fixtures 6/6, 7/7, 10/10).
- Not run locally: demo.sh, compose-render/topology/coverage/template-claims (claims.py is not read by any of them;
  CI validate runs them).
- M5 transcript: /private/tmp/claude-501/-Users-yosefgamble-github-vizra/7321b930-95d2-4a95-ba2e-61ef2ce3c13c/scratchpad/m5-transcript-8e2a867.txt — baseline 0; M5 --check 1 (regenerated would print 81,250,349,424,441 / 57);
  restored 0, clean tree; zero-match (select head pipe -> here-string) write 2, CLAIMS.md sha unchanged; same M5 at ddec39c --check 0.
- Pushed ddec39c..8e2a867 fast-forward to origin/feat/m0-compose-topology.
- CI on 8e2a867 (not re-run): validate success (job 107294690393, run 35894428837; RESULT 95 passed/0 failed/50 cases,
  topology 13 shapes/27 rules/0 violations, tree clean); ci-required success (job 107294689656, run 35894428778;
  "every required check succeeded on 8e2a867…"); GitGuardian failure = historical incident 37486665 at a96f188 (demo.sh), not required.
- PR #4 body updated (removed "never quietly wrong"/"guarantees" and "part of the local lane"; S-4 line numbers replaced
  by a pointer to CLAIMS.md; F-1 red/green table added; CI line pinned to 8e2a867).
- Worktree meta-pr4-f1 and local branch f1/meta-pr4-compose-topology removed; scratch clone deleted.
- Residual for chair: docs/quality/COMMANDS.md:654-655 still hand-writes the same six line numbers (no guarantee attached).

## Handoff
READY_FOR_REVIEW at 8e2a867. Next: independent verifier re-runs M5 against 8e2a867.
