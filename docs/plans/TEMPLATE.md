# Execution plan: <acceptance ID and outcome>

## Context
Base commit, branch/worktree, relevant issue and requirement/design references.

## Acceptance
Observable success, failure, persistence, privacy/roles, concurrency/retry, and operational requirements. Explicit non-goals for this slice; broader required features remain in the ledger.

## Implementation
Affected tables/migrations/sqlc queries, OpenAPI operations, domain/API components, UI controls, workers, and deployment configuration. Interfaces shared with other workstreams and their owner.

## Verification
Exact commands, fixtures/services/credentials, production-mode browser journey, negative and regression cases, provider/interoperability qualification, expected artifacts. Do not list commands that do not exist without marking them to be implemented first.

## Progress and evidence
Append what actually ran, on which revision/environment, outcomes and artifact paths. Separate implemented from verified. Explain test changes.

## Blockers and handoff
Smallest missing external input, reproducible failure, previous attempts, and next action. No repeated full-repository audit when this plan is sufficient.
