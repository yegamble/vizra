# Open questions with conservative proposed decisions

Status vocabulary: **PROPOSED** (applied provisionally in the spec/ledger, awaiting owner ratification) · **BLOCKED** (cannot proceed without external input) · **DECIDED** (owner ruled; date). Nothing here reduces charter scope without an explicit owner decision. Referenced from `docs/quality/features.json` `unresolved` fields. Created 2026-09-15.

| ID | Question | Evidence | Conservative proposed decision | Status |
|---|---|---|---|---|
| Q-001 | Does `vizra-search` ship as a separate repo/image from day one, or start inside `vizra-core`? | Owner named vizra-search on 2026-09-15; the kit warned against splitting search prematurely; Vidra's search is a separate internal service with SQL fallback. | Create the `vizra-search` repository now with the Vidra contract (ranked IDs, HMAC, never a hard dependency) but allow milestone M2 search to be implemented in core's SQL first; the service repo becomes mandatory by M4. Core keeps the SQL fallback forever. | PROPOSED |
| Q-002 | Create `vizra-branding` (brand mark, tokens, Figma references) as a separate repo like `vidra-branding`? | Vidra has one; DESIGN_BRIEF wants approved tokens mirrored in the repo. | Yes, after Prompt 02 produces an accepted brand; until then tokens live in `vizra-user/packages/ui`. | PROPOSED |
| Q-003 | Echo v4 or v5? | Official docs present v5 (`echo/v5`, Go ≥1.25) on 2026-09-15; kit text and council files said v4. | Pin **Echo v5** on Go 1.27.x; ADR in M0 records it; council files updated to say "Echo (v5)". | PROPOSED |
| Q-004 | Redis ≥8 is RSALv2/SSPLv1/AGPLv3. Ship Redis 8 under AGPLv3, pin Redis 7.2 (BSD), or use Valkey? | redis.io/legal/licenses; Vizra is self-hosted and distributes a compose topology. | Default the managed container to **Valkey** (BSD-3) if compatibility is verified in M0; otherwise Redis 7.2.x. Keep the client protocol-compatible so EXTERNAL can be any Redis-compatible service. Record license review. | PROPOSED |
| Q-005 | Is Cloudflare Turnstile a shipped Chevereto CAPTCHA option? | features page claims it; admin manual and compare page list reCAPTCHA v2/v3 + hCaptcha only. | Treat Turnstile as **not confirmed shipped**; Vizra still supports it (cheap, widely used) as an additional provider, evidence required per provider. | PROPOSED |
| Q-006 | Tags marked "upcoming" on chevereto.com/features but documented as shipped everywhere else. | Compare page, admin Tags dashboard, 4.5.6 changelog. | Treat tags as shipped parity; keep the conflict recorded. | PROPOSED |
| Q-007 | Per-user storage quotas: features page claims "Individual storage and access limits"; admin docs show only per-storage capacity. | CHEV-FEATURES vs CHEV-USERS-SETTINGS. | Implement per-role default quotas plus per-user overrides (VZ-QUOTA-001) — an explicit Vizra outcome regardless of the conflict. | PROPOSED |
| Q-008 | Multitenancy scope: full Chevereto tenants/plans API with per-tenant isolation, or a reduced "multi-site" model? | Compare page lists multitenancy un-columned; Tenants API documented; 4.5.5 database isolation; tenancy is Chevereto's SaaS surface. | Keep **VZ-TENANT-001/002 in the full profile** with schema-per-tenant isolation as the first mode; database-per-tenant later. Not in core. Owner may DECLINE with reason. | PROPOSED |
| Q-009 | Video depth: store + poster + native playback only, or transcoding ladder? | Chevereto: accepts MP4/MOV/WebM and generates a frame; no transcoding documented. Flickr: 1080p, length caps. | Store + poster + bounded browser-native playback in core; **no transcoding** unless owner asks. Video length/size limits configurable. | PROPOSED |
| Q-010 | HEIC/HEIF and RAW support? | Chevereto: neither documented. Flickr: HEIC→JPEG in app; RAW unsupported. libvips/govips can decode HEIC (license/patent caveats). | Not required for parity. Offer **HEIC → JPEG/AVIF conversion on upload** as a full-profile option gated on a documented libvips build; RAW declined. | PROPOSED |
| Q-011 | External comment providers (Disqus/JS) alongside native comments? | Chevereto only has external; Vizra requires native. | Native comments are mandatory. An optional external embed is allowed only as a CSP-scoped provider allowlist (VZ-SERVICES-005), never raw HTML. | PROPOSED |
| Q-012 | Which of the 30 Chevereto OAuth providers must be first-class? | CHEV-LOGIN-PROVIDERS; generic OIDC covers many. | Generic OAuth2/OIDC adapter + first-class Google, GitHub, Discord, Apple, Microsoft; others via generic config with live evidence per provider before being listed as supported. | PROPOSED |
| Q-013 | Banner/ad slots: allow third-party scripts? | Chevereto injects arbitrary HTML/JS; AGENTS.md forbids unrestricted script surfaces. | Sanitized HTML/images by default; script-based ad providers only via an explicit CSP-scoped allowlist the owner enables with a warning. | PROPOSED |
| Q-014 | Custom JS, PHP pages, arbitrary analytics HTML — how much customization parity? | CHEV-THEME, CHEV-PAGES, CHEV-EXTERNAL-SERVICES vs AGENTS.md. | **Intentional difference:** sanitized custom CSS, rich-text/sanitized-HTML pages, typed theme tokens, analytics providers allowlist; no raw JS/server code. Recorded as approved deviation pending owner sign-off. | PROPOSED |
| Q-015 | Expiring uploads: allowed range and deletion guarantee? | Chevereto accepts ISO-8601 durations; range undocumented; user setting 5 min–1 year. | Range 5 minutes–1 year; deletion by durable job within 15 minutes of expiry; documented as best-effort for third-party copies. | PROPOSED |
| Q-016 | Backblaze S3-compatible checksum headers and multipart limits. | No reachable official page. | Adapter makes checksum algorithm per-provider configurable; Backblaze lane records observed behavior; until then UNVERIFIED. | BLOCKED (needs live lane) |
| Q-017 | Minimum Compose version for `!reset`/`!override`. | Docker docs do not state; Vidra verified 2.24. | Require Compose ≥ 2.24, verified empirically in M0 CI (render assertion), not by citation. | PROPOSED |
| Q-018 | Unify likes and favorites? | Chevereto "liked" = "favourited"; Flickr faves binary; charter allows deliberate unification. | **Unify** into one action named Favorite (personal listing + public count + notification). No separate public like. Ratings remain distinct. | PROPOSED |
| Q-019 | Flickr Pro-gated capabilities (stats, originals, 6K, long video, groups cap, API keys): all ungated in Vizra? | FLICKR-FREE-LIMITS, FLICKR-PRO. | Yes — every capability ungated, subject to operator policy/quotas. No billing project. | PROPOSED |
| Q-020 | View count definition: impression-based (Flickr) or page-view-based? | FLICKR-STATS. | Page-view/asset-open based, documented; impressions optional in analytics. | PROPOSED |
| Q-021 | CSAM detection (Project Arachnid Shield-class) needs provider credentials to test. | CHEV-EXTERNAL-SERVICES. | Build the adapter and contract tests; live evidence BLOCKED until the owner obtains access; ledger stays open. | BLOCKED (credentials) |
| Q-022 | ModerateContent service availability and terms for testing. | CHEV-EXTERNAL-SERVICES. | Adapter interface with a sandbox provider; live evidence when credentials exist. | BLOCKED (credentials) |
| Q-023 | Personal/single-profile site mode: in core or full? | CHEV-WEBSITE. | Full profile (VZ-ADMIN-003 covers it; core needs only public/private site mode). | PROPOSED |
| Q-024 | Flood-protection thresholds and rate-limit defaults. | CHEV-FLOOD recommended values. | Ship Chevereto's recommended values as defaults; configurable. | PROPOSED |
| Q-025 | CDN caching of private assets. | ARCH; CHEV-SYSTEM CDN. | Private and unlisted assets are never CDN-cacheable; only public derivatives with visibility-versioned keys. | PROPOSED |
| Q-026 | Developer tooling parity (xrDebug, REPL, debug levels). | CHEV-COMPARE system features. | DECLINE as product features; Vizra provides structured logs, metrics, tracing and `vizra doctor`. Owner to confirm. | PROPOSED |
| Q-027 | ARM64 support claim. | Charter: define AMD64 first. | AMD64 only until a native ARM64 lane exists. | PROPOSED |
| Q-028 | Reference hardware, dataset and performance/RPO/RTO budgets. | RELEASE_ACCEPTANCE. | Propose in M0 ADR: 2 vCPU/4 GB reference, 10k-photo fixture, p95 < 300 ms metadata reads, upload-to-thumbnail < 30 s p95, RPO 24 h (nightly) / RTO 2 h; owner approves numbers. | PROPOSED |
| Q-029 | Forum plugins (XenForo, Discourse, MyBB, phpBB, bbPress, IPB, Discuz!, NodeBB, SMF, Vanilla, vBulletin, WoltLab) parity. | CHEV-FEATURES, CHEV-USER-PUP vendors. | One embeddable uploader with a documented plugin contract and vendor presets; per-forum plugin packages are separate full-profile deliverables prioritized by owner. | PROPOSED |
| Q-030 | Legacy Backblaze native B2 adapter. | CHEV-COMPARE Pro row. | Satisfy via S3-compatible unless owner requires native. | PROPOSED |
| Q-031 | Native GCS/Azure/OSS/Swift/SFTP/FTP adapters: native SDKs or S3-compatible gateways where vendors offer them? | CHEV-COMPARE Pro rows. | Native for GCS, Azure, Swift, SFTP, FTP; OSS via S3-compatible endpoint if its S3 API passes the contract suite. FTP requires an insecure-transport acknowledgement. | PROPOSED |
| Q-032 | Branch protection/merge-queue configuration requires GitHub admin rights. | DOD. | Record required configuration in docs; enforcement UNVERIFIED until an admin applies it. | BLOCKED (permissions) |
| Q-033 | API key scopes and rate limits (Chevereto documents none). | CHEV-API-V4. | Scopes upload/read/manage; per-key and per-user limits configurable; documented. | PROPOSED |
| Q-034 | Image engine: govips (libvips, LGPL) vs pure Go. | RES-PROTOCOLS. | **govips/libvips** for the worker (formats, performance); LGPL notice preserved in images; pure-Go fallback only for JPEG/PNG in tests. | PROPOSED |
| Q-035 | Chevereto import: which schema versions? | Chevereto 4.x MySQL. | Support Chevereto 4.x exports; older versions refused with guidance. | PROPOSED |
| Q-036 | AT Protocol native records: Lexicon NSID namespace. | AT-LEXICON. | Namespace under a domain the owner controls (e.g. `<owner-domain>.vizra.*`), decided before M6. | BLOCKED (owner domain) |
| Q-037 | Gallery limits and opt-out granularity. | FLICKR-GALLERIES. | 500 items; global opt-out plus per-item override. | PROPOSED |
| Q-038 | Geotag/map feature scope (tile provider, privacy). | FLICKR-GEO. | Full profile; self-hostable tiles or configurable provider; geo-privacy default "only me". | PROPOSED |
| Q-039 | Privacy model: per-item visibility + audiences (followers/named lists) beyond Chevereto's album-only privacy. | CHEV-USER-ALBUM vs FLICKR-PRIVACY. | Per-item visibility (public/unlisted/private) in core; named audiences and follower-only in full. Album never widens. | PROPOSED |
| Q-040 | Private profiles: follow requires approval? | CHEV private profile; Flickr no approval. | Private profile → follow requests with approval (full); core: private profile hides identity only. | PROPOSED |
| Q-041 | Notes and people tagging in scope? | FLICKR-NOTES, FLICKR-PEOPLE. | Full profile, consent-based; owner may DECLINE. | PROPOSED |
| Q-042 | Direct messages in scope? | FLICKR-MAIL. | Full profile, 1:1 only, no E2EE claims; owner may DECLINE. | PROPOSED |
| Q-043 | Locale list for launch. | CHEV-LANGUAGES 34 locales. | English at core; i18n framework in core; translations added per locale with reviewers in full. | PROPOSED |

## Approved-difference register (competitor mechanism vs Vizra outcome)
| Reference mechanism | Vizra outcome | Why | Owner sign-off |
|---|---|---|---|
| Chevereto Disqus/JS comments | Native comments; optional allowlisted provider | Charter requires native comments | pending |
| Chevereto album-only privacy | Per-item visibility + album privacy + grants | Charter/Flickr workflows; default-deny | pending |
| Chevereto raw JS / PHP pages / HTML injection | Sanitized CSS/HTML, typed extension points | AGENTS.md guardrail | pending (Q-014) |
| Chevereto web-triggered upgrades | UI shows availability; CLI performs upgrade | Privilege separation | pending |
| Chevereto likes + Flickr faves | One Favorite action; separate numeric ratings | Charter allows deliberate unification | pending (Q-018) |
| Flickr Pro paywall | All capabilities ungated under operator policy | Charter forbids billing project | pending (Q-019) |
| Flickr desktop-only collections/organizr | Mobile-capable | Completeness | pending |
| Flickr empty-album auto-delete | Empty albums allowed; delete never deletes photos | Data-loss avoidance | pending |
