
########## DEMO 9 — every way to neuter the REAL validate lane (FINDINGS 3, 7) ##########

BASE SHA: c47c4ba8b931c4700c5f96e071295afce1f97fb4

continue-on-error is demo 6. These are the five other constructs, each
applied to the real .github/workflows/validate.yml, not only to fixtures.

########## RED 9a — workflow-level defaults.run.shell ##########

  sha256(.github/workflows/validate.yml) = 68031b0719ceba2f
inserted workflow-level defaults.run.shell
$ ./scripts/ci-required-guard.sh
required checks:
  - validate
workflows parsed: 2; no continue-on-error on any job or step
workflows parsed: 2; 3 action reference(s), every one pinned to a 40-hex commit SHA; every required lane runs on pull_request for any base branch
floor: every non-optional lane is present in the manifest
manifest entries are bare job names
VIOLATION .github/workflows/validate.yml reason=defaults-shell-override where=workflow shell='bash -c "eval \\"$@\\" || true" --'
  `defaults.run.shell` replaces the shell every `run:` step executes in. A shell
  that swallows non-zero exits turns every check in this lane into a no-op that
  still reports success — the Actions analogue of `SHELL := /usr/bin/true`.
a required lane is undefined, shell-overridden, or has a conditional step
EXIT=1

########## RED 9b — job-level defaults.run.shell ##########

inserted job-level defaults.run.shell
$ ./scripts/ci-required-guard.sh
required checks:
  - validate
workflows parsed: 2; no continue-on-error on any job or step
workflows parsed: 2; 3 action reference(s), every one pinned to a 40-hex commit SHA; every required lane runs on pull_request for any base branch
floor: every non-optional lane is present in the manifest
manifest entries are bare job names
VIOLATION .github/workflows/validate.yml reason=defaults-shell-override where=jobs.validate shell='bash -c "eval \\"$@\\" || true" --'
  `defaults.run.shell` replaces the shell every `run:` step executes in. A shell
  that swallows non-zero exits turns every check in this lane into a no-op that
  still reports success — the Actions analogue of `SHELL := /usr/bin/true`.
a required lane is undefined, shell-overridden, or has a conditional step
EXIT=1

########## RED 9c — step-level shell: (NEW in round 2) ##########

inserted step-level shell: on the ledger step
$ ./scripts/ci-required-guard.sh
required checks:
  - validate
workflows parsed: 2; no continue-on-error on any job or step
workflows parsed: 2; 3 action reference(s), every one pinned to a 40-hex commit SHA; every required lane runs on pull_request for any base branch
floor: every non-optional lane is present in the manifest
manifest entries are bare job names
VIOLATION .github/workflows/validate.yml reason=step-shell-override lane=validate where=jobs.validate.steps[3] shell='bash -c "eval \\"$@\\" || true" --'
  A step-level `shell:` is exactly the `defaults.run.shell` rule with the key
  in a different place: a shell that swallows non-zero exits makes this step
  report success whatever the command does.
a required lane is undefined, shell-overridden, or has a conditional step
EXIT=1

########## RED 9d — step-level 'if: false' ##########

inserted if: false
$ ./scripts/ci-required-guard.sh
required checks:
  - validate
workflows parsed: 2; no continue-on-error on any job or step
workflows parsed: 2; 3 action reference(s), every one pinned to a 40-hex commit SHA; every required lane runs on pull_request for any base branch
floor: every non-optional lane is present in the manifest
manifest entries are bare job names
VIOLATION .github/workflows/validate.yml reason=constant-false-if lane=validate where=jobs.validate.steps[3] if='False'
  Step 'the generated ledger matches its sources' carries an `if:` on a REQUIRED lane.
  It is a constant false, so this step never runs at all while the lane
  still reports success. A skipped JOB is refused by the fan-in; a skipped
  STEP leaves the job green, which is why it is refused here.
a required lane is undefined, shell-overridden, or has a conditional step
EXIT=1

########## RED 9e — a step-level 'if:' nothing can evaluate ##########

inserted a non-constant if:
  111:        if: ${{ github.event.repository.owner.login == 'nobody' }}
$ ./scripts/ci-required-guard.sh
required checks:
  - validate
workflows parsed: 2; no continue-on-error on any job or step
workflows parsed: 2; 3 action reference(s), every one pinned to a 40-hex commit SHA; every required lane runs on pull_request for any base branch
floor: every non-optional lane is present in the manifest
manifest entries are bare job names
VIOLATION .github/workflows/validate.yml reason=conditional-step-on-required-lane lane=validate where=jobs.validate.steps[3] if="${{ github.event.repository.owner.login == 'nobody' }}"
  Step 'the generated ledger matches its sources' carries an `if:` on a REQUIRED lane.
  Nothing here can evaluate that expression, and a step that MIGHT not run
  is not a gate. A required lane's steps run unconditionally.
a required lane is undefined, shell-overridden, or has a conditional step
EXIT=1

########## RED 9f — runs-on a self-hosted runner (NEW in round 2) ##########

switched the required lane to a self-hosted runner
$ ./scripts/ci-required-guard.sh
required checks:
  - validate
workflows parsed: 2; no continue-on-error on any job or step
workflows parsed: 2; 3 action reference(s), every one pinned to a 40-hex commit SHA; every required lane runs on pull_request for any base branch
floor: every non-optional lane is present in the manifest
manifest entries are bare job names
VIOLATION .github/workflows/validate.yml reason=self-hosted-runner lane=validate runs-on=['self-hosted', 'linux', 'x64']
  A required lane must run on the GitHub-hosted target the contract names
  (ubuntu-24.04; ADR-009/Q-027). A self-hosted runner is a machine this
  repository does not describe, cannot reproduce and does not control, so a green
  check from it does not mean what a green check is supposed to mean.
a required lane is undefined, shell-overridden, or has a conditional step
EXIT=1

########## RED 9g — the lane replaced by a reusable-workflow call (NEW in round 2) ##########

replaced the validate job with a reusable-workflow call
$ ./scripts/ci-required-guard.sh
required checks:
  - validate
workflows parsed: 2; no continue-on-error on any job or step
workflows parsed: 2; 2 action reference(s), every one pinned to a 40-hex commit SHA; every required lane runs on pull_request for any base branch
floor: every non-optional lane is present in the manifest
manifest entries are bare job names
VIOLATION .github/workflows/validate.yml reason=reusable-workflow lane=validate uses='./.github/workflows/elsewhere.yml'
  This required lane is a call to a reusable workflow, so what it actually runs
  lives somewhere this checker cannot see: every rule here — shell overrides,
  conditional steps, action pins — would be enforced against an empty job while
  the real steps went unchecked.
a required lane is undefined, shell-overridden, or has a conditional step
EXIT=1

########## RED 9h — the required lane's job renamed ##########

renamed the validate job
$ ./scripts/ci-required-guard.sh
required checks:
  - validate
workflows parsed: 2; no continue-on-error on any job or step
workflows parsed: 2; 3 action reference(s), every one pinned to a 40-hex commit SHA; every required lane runs on pull_request for any base branch
floor: every non-optional lane is present in the manifest
manifest entries are bare job names
VIOLATION reason=no-such-job lane=validate
  Required check 'validate' has no job of that name in any of 2 workflow file(s). The fan-in would wait for a check nobody defined.
a required lane is undefined, shell-overridden, or has a conditional step
EXIT=1

########## GREEN ##########

  sha256(.github/workflows/validate.yml) = 68031b0719ceba2f
$ ./scripts/ci-required-guard.sh
required checks:
  - validate
workflows parsed: 2; no continue-on-error on any job or step
workflows parsed: 2; 3 action reference(s), every one pinned to a 40-hex commit SHA; every required lane runs on pull_request for any base branch
floor: every non-optional lane is present in the manifest
manifest entries are bare job names
lane integrity: 1 required lane(s) defined by a real job across 2 workflow file(s); GitHub-hosted, steps in this repository, no shell override at workflow/job/step level, no conditional step
the workflow checker rejects every continue-on-error spelling in scripts/testdata/ (6 fixtures exercised, floor 6)
the pin checker rejects every unpinned-action and untriggered-lane spelling in scripts/testdata/ (7 fixtures exercised, floor 7)
the lane checker rejects every shell-override (workflow/job/step), conditional-step, self-hosted-runner, reusable-workflow and missing-job case in scripts/testdata/ (10 fixtures exercised, floor 10)
EXIT=0
