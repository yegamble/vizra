# Generated by the 8 required local lane commands in docs/quality/COMMANDS.md
# CODE COMMIT UNDER TEST: e82e659e251356a55c94fc36a0f6dc0730239808
#
# Committed SEPARATELY, in an evidence-only commit on top of the code commit
# named above: a transcript cannot name the commit that contains it.
# Generated: 2026-09-21T09:10:50Z

### environment
arm64
Python 3.9.6
Docker version 29.8.0, build 88096ef
docker compose 5.5.1
bash GNU bash, version 3.2.57(1)-release (arm64-apple-darwin25)

### $ ./scripts/check-generated-ledger.sh
regenerating with: Python 3.9.6
OK 191 requirements; core=141; areas={'foundation': 8, 'ci': 5, 'install': 4, 'topology': 8, 'operations': 8, 'backup': 4, 'upgrade': 2, 'identity': 14, 'upload': 13, 'media': 13, 'storage': 15, 'import': 4, 'activitypub': 4, 'atproto': 2, 'ipfs': 2, 'organization': 24, 'community': 19, 'search': 12, 'admin': 26, 'design': 4}; questions=['Q-016', 'Q-021', 'Q-022', 'Q-028', 'Q-032', 'Q-036']; decided=['Q-001', 'Q-002', 'Q-003', 'Q-004', 'Q-005', 'Q-006', 'Q-007', 'Q-008', 'Q-009', 'Q-010', 'Q-011', 'Q-012', 'Q-013', 'Q-014', 'Q-015', 'Q-017', 'Q-018', 'Q-023', 'Q-024', 'Q-027', 'Q-028', 'Q-029', 'Q-030', 'Q-031', 'Q-033', 'Q-034', 'Q-035', 'Q-037', 'Q-038', 'Q-039', 'Q-040', 'Q-041', 'Q-042', 'Q-043']
locale regression: re-running under LC_ALL=C LANG=POSIX PYTHONCOERCECLOCALE=0 PYTHONUTF8=0
generated ledger reproduces byte-for-byte: docs/quality/features.json (UTF-8 and C/POSIX locales)
exit=0

### $ ./scripts/check-quality-json.py
json parsed: 4 file(s) under docs/quality/
ledger: 191 requirement id(s) in docs/quality/features.json
requirement ids: 191 written reference(s) expanded to 287 id(s), 204 distinct, across 14 document(s); every one resolves
exit=0

### $ ./scripts/check-doc-links.py
relative links: none exist yet across 95 markdown file(s) (checked nothing); 7 external URL(s) recorded and not fetched
compose comments: 6 env//deploy/ path(s) across 8 file(s), all resolve (2 allow-listed as generated-and-absent)
exit=0

### $ ./scripts/ci-required-guard.sh
required checks:
  - validate
workflows parsed: 2; no continue-on-error on any job or step
workflows parsed: 2; 4 action reference(s), every one pinned to a 40-hex commit SHA; every required lane runs on pull_request for any base branch
floor: every non-optional lane is present in the manifest
manifest entries are bare job names
lane integrity: 1 required lane(s) defined by a real job across 2 workflow file(s); GitHub-hosted, steps in this repository, no shell override at workflow/job/step level, no conditional step
the workflow checker rejects every continue-on-error spelling in scripts/testdata/ (6 fixtures exercised, floor 6)
the pin checker rejects every unpinned-action and untriggered-lane spelling in scripts/testdata/ (7 fixtures exercised, floor 7)
the lane checker rejects every shell-override (workflow/job/step), conditional-step, self-hosted-runner, reusable-workflow and missing-job case in scripts/testdata/ (10 fixtures exercised, floor 10)
exit=0

### $ ./scripts/compose-render.py --all --out build/compose-models
docker compose 5.5.1 (parsed 5.5.1) meets the floor 2.24.4 (Q-017)
  rendered dev-default                -> build/compose-models/dev-default.json
  rendered dev-build-from-checkouts   -> build/compose-models/dev-build-from-checkouts.json
  rendered prod-default               -> build/compose-models/prod-default.json
  rendered prod-external-tls          -> build/compose-models/prod-external-tls.json
  rendered prod-worker-split          -> build/compose-models/prod-worker-split.json
  rendered prod-frontend-only         -> build/compose-models/prod-frontend-only.json
  rendered prod-all-optional          -> build/compose-models/prod-all-optional.json
  rendered prod-external-postgres     -> build/compose-models/prod-external-postgres.json
  rendered prod-external-redis        -> build/compose-models/prod-external-redis.json
  rendered prod-external-both         -> build/compose-models/prod-external-both.json
  rendered prod-external-clickhouse   -> build/compose-models/prod-external-clickhouse.json
  rendered prod-external-ipfs         -> build/compose-models/prod-external-ipfs.json
  rendered bundle-no-checkouts        -> build/compose-models/bundle-no-checkouts.json
exit=0

### $ ./scripts/check-compose-topology.py build/compose-models

KNOWN-FALSE PROBES: 2 service(s) carry a healthcheck that CANNOT GO RED while the service is broken.
  - api: CMD /usr/local/bin/vizra version  [present in the rendered model]
      why it is false: a CLI that prints build info and exits 0 without opening a socket or touching PostgreSQL. An api wedged with PostgreSQL unreachable, or listening but deadlocked, still reports healthy.
      emptied by:      vizra-core `vizra healthcheck` subcommand (queue 2h), mirroring vizra-search's
  - worker: CMD /usr/local/bin/vizra version  [present in the rendered model]
      why it is false: same binary, same reason; the worker additionally has no HTTP listener on the API port at all. Its metrics listener on VIZRA_METRICS_ADDR is the thing a real probe would reach.
      emptied by:      vizra-core `vizra healthcheck` subcommand (queue 2h)
  No depends_on service_healthy edge may point at one (rule probe-gates-readiness), and this list must be empty before VZ-ISSUE-004's boot lane lands.
  The operator-facing half of this admission — `docker compose ps` reports these
  services healthy whatever their state — is carried by:
    env/production.env.example
    README.md
  Those files and this list are deleted together: rule
  known-false-undisclosed fails while the list stands and a paragraph is
  missing, and rule known-false-stale-disclosure fails once the list is
  empty and a paragraph survives.
  SCOPE: probe-gates-readiness only refuses a gate onto a probe
  DECLARED false above. It cannot tell a real probe from a fake one
  nobody declared; gated-probe-unrecognised is what covers that, by
  requiring every service_healthy target's RENDERED probe to run a
  declared command as its own command - one plain CMD or CMD-SHELL
  invocation, no pipeline, comment, redirection or second statement.
  It still cannot show that the command can exit NON-ZERO
  (`pg_isready --version` would pass) or that a binary with the right
  name is the real one. That needs a running container, which is
  VZ-ISSUE-004's boot lane.

compose topology: 13 shape(s) asserted from their rendered models; 27 rules, 0 violations; 2 known-false probe(s) named above
exit=0

### $ ./scripts/check-config-coverage.py build/compose-models
ALIASES: 1 operator key(s) feeding two service spellings
  - VIZRA_PUBLIC_ORIGIN -> vizra-core:VIZRA_PUBLIC_ORIGIN, vizra-user:PUBLIC_ORIGIN
      authorised: True  by chair ruling 2026-09-21, WARROOM-BOARD queue 2h
      removed when: vizra-user main reads VIZRA_PUBLIC_ORIGIN
config coverage: 34 component keys across 3 components, 58 template keys, 58 interpolated variables, 13 shapes; 1 declared alias(es); 1 retired key(s) refused; 0 violations
exit=0

### $ ./scripts/check-template-claims.py
template claims: 10 operator-facing file(s), 16 command and script reference(s); 5 shipped command(s), 11 declared future command(s), 0 script reference(s) resolved to a file in the tree and 6 script(s) declared future; 0 violations
exit=0

### $ python3 docs/evidence/compose-topology/claims.py --check
CLAIMS.md is current (47 audited claims)
exit=0

### $ ./scripts/check-config-coverage.py --drift   # component checkouts ABSENT
BLOCKED: the drift check needs the component checkouts and these are absent:
  - vizra-core
  - vizra-user
  - vizra-search
  This is BLOCKED, not a pass and not a skip. Run bootstrap.sh (or
  clone the components) and run this again. It cannot run in this
  repository's CI at all: there is no token for a private sibling
  repository, which is why env/registry/*.json are snapshots.
exit=2

### BLOCKED, recorded as BLOCKED
`--drift` exited 2 above because the component checkouts are absent, which is
the CI situation: there is no token for a private sibling repository. It is not
a pass and not a skip. The red/green run with the checkouts present is in
drift-transcript.txt.

### S-2 residual, MEASURED rather than asserted
`gated-probe-unrecognised` is a structural check over the rendered probe. It
cannot show that the declared command can exit NON-ZERO. Substituting
`["CMD-SHELL", "pg_isready --version"]` for postgres's probe — a command that
always succeeds — and running the topology checker:
  check-compose-topology.py exit=0   <- GREEN. This is the documented limit.
  restored; tree dirty files, excluding the transcripts being written now: 0

### the harness FAILS a guard that does not produce its declared signal
S-4's fix replaced `printf … | grep -q` with a here-string. The acceptance
criterion is that this must not make a condition unconditionally true. Asserting
a rule id that the mutation does not produce:
  the real signal IS present:            rule=never-published service=postgres
  an ABSENT signal is still reported absent: rule=this-rule-does-not-exist -> no match
  so `grep -qF ... <<< "$out"` still returns 1, and a broken guard is still FAIL.
  restored; tree dirty files, excluding the transcripts being written now: 0

### the two registry fields any checker consumes
CLAIMS.md asserts that exactly `name` and `secret` are read from a registry
`keys[]` entry. Every access, across every script:
  scripts/check-config-coverage.py:235:        declared = {k["name"] for k in reg["keys"]}
  scripts/check-config-coverage.py:330:        for k in reg["keys"]:
  scripts/check-config-coverage.py:331:            if k.get("secret"):
  scripts/check-config-coverage.py:332:                flagged.add(k["name"])
  scripts/check-config-coverage.py:449:            if reg is None or sk["key"] not in {k["name"] for k in reg["keys"]}:
  scripts/check-config-coverage.py:537:            {k["name"] for k in reg["keys"]}
  scripts/check-config-coverage.py:564:                k["name"] for k in reg["keys"] if k.get("secret")
  scripts/check-config-coverage.py:572:        for k in reg["keys"]:
  scripts/check-config-coverage.py:573:            name = k["name"]
  scripts/check-config-coverage.py:582:            if bool(k.get("secret")) != flags[name]:
  scripts/check-config-coverage.py:583:                snap = "secret" if k.get("secret") else "not secret"
  scripts/compose-render.py:301:            if k.get("secret"):
  scripts/compose-render.py:302:                names.add(k["name"])

