# Generated by bash docs/evidence/compose-topology/demo.sh
# CODE COMMIT UNDER TEST: a96f18895f38594164bc216b2f9727d63a10ecf9
#
# This transcript is committed SEPARATELY, in an evidence-only commit on top of
# the code commit named above. A transcript cannot name the commit that contains
# it, so the previous round shipped transcripts stamped with a superseded SHA.
# The SHA above is the tree these results were produced from; this file's own
# commit adds nothing but this file.
# Generated: 2026-09-21T06:49:52Z

### environment
arm64
Python 3.9.6
Docker version 29.8.0, build 88096ef
docker compose 5.5.1

### $ ./scripts/check-generated-ledger.sh
regenerating with: Python 3.9.6
OK 191 requirements; core=141; areas={'foundation': 8, 'ci': 5, 'install': 4, 'topology': 8, 'operations': 8, 'backup': 4, 'upgrade': 2, 'identity': 14, 'upload': 13, 'media': 13, 'storage': 15, 'import': 4, 'activitypub': 4, 'atproto': 2, 'ipfs': 2, 'organization': 24, 'community': 19, 'search': 12, 'admin': 26, 'design': 4}; questions=['Q-016', 'Q-021', 'Q-022', 'Q-028', 'Q-032', 'Q-036']; decided=['Q-001', 'Q-002', 'Q-003', 'Q-004', 'Q-005', 'Q-006', 'Q-007', 'Q-008', 'Q-009', 'Q-010', 'Q-011', 'Q-012', 'Q-013', 'Q-014', 'Q-015', 'Q-017', 'Q-018', 'Q-023', 'Q-024', 'Q-027', 'Q-028', 'Q-029', 'Q-030', 'Q-031', 'Q-033', 'Q-034', 'Q-035', 'Q-037', 'Q-038', 'Q-039', 'Q-040', 'Q-041', 'Q-042', 'Q-043']
locale regression: re-running under LC_ALL=C LANG=POSIX PYTHONCOERCECLOCALE=0 PYTHONUTF8=0
generated ledger reproduces byte-for-byte: docs/quality/features.json (UTF-8 and C/POSIX locales)
exit=0

### $ ./scripts/check-quality-json.py
json parsed: 4 file(s) under docs/quality/
ledger: 191 requirement id(s) in docs/quality/features.json
requirement ids: 191 written reference(s) expanded to 287 id(s), 204 distinct, across 14 document(s); every one resolves
exit=0

### $ ./scripts/check-doc-links.py
relative links: none exist yet across 94 markdown file(s) (checked nothing); 7 external URL(s) recorded and not fetched
compose comments: 6 env//deploy/ path(s) across 8 file(s), all resolve (2 allow-listed as generated-and-absent)
exit=0

### $ ./scripts/ci-required-guard.sh
required checks:
  - validate
workflows parsed: 2; no continue-on-error on any job or step
workflows parsed: 2; 4 action reference(s), every one pinned to a 40-hex commit SHA; every required lane runs on pull_request for any base branch
floor: every non-optional lane is present in the manifest
manifest entries are bare job names
lane integrity: 1 required lane(s) defined by a real job across 2 workflow file(s); GitHub-hosted, steps in this repository, no shell override at workflow/job/step level, no conditional step
the workflow checker rejects every continue-on-error spelling in scripts/testdata/ (6 fixtures exercised, floor 6)
the pin checker rejects every unpinned-action and untriggered-lane spelling in scripts/testdata/ (7 fixtures exercised, floor 7)
the lane checker rejects every shell-override (workflow/job/step), conditional-step, self-hosted-runner, reusable-workflow and missing-job case in scripts/testdata/ (10 fixtures exercised, floor 10)
exit=0

### $ ./scripts/compose-render.py --all --out build/compose-models
  rendered dev-build-from-checkouts   -> build/compose-models/dev-build-from-checkouts.json
  rendered prod-default               -> build/compose-models/prod-default.json
  rendered prod-external-tls          -> build/compose-models/prod-external-tls.json
  rendered prod-worker-split          -> build/compose-models/prod-worker-split.json
  rendered prod-frontend-only         -> build/compose-models/prod-frontend-only.json
  rendered prod-all-optional          -> build/compose-models/prod-all-optional.json
  rendered prod-external-postgres     -> build/compose-models/prod-external-postgres.json
  rendered prod-external-redis        -> build/compose-models/prod-external-redis.json
  rendered prod-external-both         -> build/compose-models/prod-external-both.json
  rendered prod-external-clickhouse   -> build/compose-models/prod-external-clickhouse.json
  rendered prod-external-ipfs         -> build/compose-models/prod-external-ipfs.json
  rendered bundle-no-checkouts        -> build/compose-models/bundle-no-checkouts.json
exit=0

### $ ./scripts/check-compose-topology.py build/compose-models

KNOWN-FALSE PROBES: 2 service(s) carry a healthcheck that CANNOT GO RED while the service is broken.
  - api: CMD /usr/local/bin/vizra version  [present in the rendered model]
      why it is false: a CLI that prints build info and exits 0 without opening a socket or touching PostgreSQL. An api wedged with PostgreSQL unreachable, or listening but deadlocked, still reports healthy.
      emptied by:      vizra-core `vizra healthcheck` subcommand (queue 2h), mirroring vizra-search's
  - worker: CMD /usr/local/bin/vizra version  [present in the rendered model]
      why it is false: same binary, same reason; the worker additionally has no HTTP listener on the API port at all. Its metrics listener on VIZRA_METRICS_ADDR is the thing a real probe would reach.
      emptied by:      vizra-core `vizra healthcheck` subcommand (queue 2h)
  No depends_on service_healthy edge may point at one (rule probe-gates-readiness), and this list must be empty before VZ-ISSUE-004's boot lane lands.

compose topology: 13 shape(s) asserted from their rendered models; 23 rules, 0 violations; 2 known-false probe(s) named above
exit=0

### $ ./scripts/check-config-coverage.py build/compose-models
ALIASES: 2 operator key(s) feeding two service spellings
  - SEARCH_HMAC_KEY -> vizra-core:VIZRA_SEARCH_HMAC_KEY, vizra-search:SEARCH_HMAC_KEY
      authorised: True  by chair ruling, VZ-ISSUE-002 brief: SEARCH_HMAC_KEY is the contract's name on BOTH sides; vizra-core's rename is queued as core PR #6.
      removed when: vizra-core `main` retires VIZRA_SEARCH_HMAC_KEY. At that point this entry is DELETED, the floor drops to 1, env/registry/core.json lists the old name in `retired_keys`, and docker-compose.yml delivers SEARCH_HMAC_KEY only - one commit, because delivering both names is a boot refusal and delivering only the new one against today's core main is also a boot refusal.
  - VIZRA_PUBLIC_ORIGIN -> vizra-core:VIZRA_PUBLIC_ORIGIN, vizra-user:PUBLIC_ORIGIN
      authorised: True  by chair ruling 2026-09-21, WARROOM-BOARD queue 2h
      removed when: vizra-user main reads VIZRA_PUBLIC_ORIGIN
config coverage: 34 component keys across 3 components, 57 template keys, 57 interpolated variables, 13 shapes; 2 declared alias(es); 0 retired key(s) refused; 0 violations
exit=0

### $ ./scripts/check-template-claims.py
template claims: 10 operator-facing file(s), 9 command reference(s); 5 shipped command(s), 11 declared future command(s); 0 violations
exit=0

