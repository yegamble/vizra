{
  "note": "ONE operator key feeding two differently-spelled service keys. Each entry is a naming conflict between component repositories, not a convenience. The floor below is a decision: adding an alias must be a visible edit, because every alias is a place where an operator can believe they configured something they did not.",
  "floor": 2,
  "aliases": [
    {
      "operator_key": "SEARCH_HMAC_KEY",
      "service_keys": [
        {
          "component": "vizra-core",
          "key": "VIZRA_SEARCH_HMAC_KEY"
        },
        {
          "component": "vizra-search",
          "key": "SEARCH_HMAC_KEY"
        }
      ],
      "authorised_by": "war-room chair ruling, VZ-ISSUE-002 brief: SEARCH_HMAC_KEY is the contract's name on BOTH sides; vizra-core's rename is queued in another slice.",
      "removed_when": "vizra-core renames VIZRA_SEARCH_HMAC_KEY to SEARCH_HMAC_KEY."
    },
    {
      "operator_key": "VIZRA_PUBLIC_ORIGIN",
      "service_keys": [
        {
          "component": "vizra-core",
          "key": "VIZRA_PUBLIC_ORIGIN"
        },
        {
          "component": "vizra-user",
          "key": "PUBLIC_ORIGIN"
        }
      ],
      "authorised_by": "NOT YET RULED ON. Found while implementing VZ-ISSUE-002 and REPORTED: it is the same shape as the HMAC conflict - two repositories spell one value differently - and it needs the same ruling. Implemented as an alias because the alternative is two operator keys that must be kept byte-identical by hand, which is a worse failure mode.",
      "removed_when": "the chair rules on one spelling and the losing repository renames."
    }
  ]
}
